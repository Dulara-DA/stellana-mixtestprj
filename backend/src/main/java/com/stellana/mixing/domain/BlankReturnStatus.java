package com.stellana.mixing.domain;

public enum BlankReturnStatus {
    RETURN_PREPARED,
    SENT_TO_BLANKING,
    AWAITING_CONFIRMATION,
    RECEIVED_BY_BLANKING,
    QUANTITY_DISPUTED,
    CLOSED
}
