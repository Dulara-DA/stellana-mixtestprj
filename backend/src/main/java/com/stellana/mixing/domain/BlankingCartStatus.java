package com.stellana.mixing.domain;

public enum BlankingCartStatus {
    PREPARED,
    HELD,
    READY_FOR_DISPATCH,
    DISPATCHED,
    RECEIVED_AT_MOULDING,
    IN_USE,
    PARTIALLY_CONSUMED,
    FULLY_CONSUMED,
    RETURN_PENDING,
    RETURNED_TO_BLANKING,
    CLOSED
}
